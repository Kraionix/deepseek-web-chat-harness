"""Pure data and rules for the harness.

No imports outside the standard library. No dependencies on `lxml`,
`pydantic`, or any other third-party package. The models here are
plain dataclasses; the rules are pure predicates over them.
"""
